--[[
	Indexing table elements by a fixed set of primitive keys

	idx = new{key,...}



	idx:index(e)
	idx:lookup(e, [keys])
	idx:remove(e)
	idx:list() -> iterator -> e

]]

local index = require 'fbclient.ds.index'

module(...)

function new()

end

_M.

function set(t, keys)

return index


